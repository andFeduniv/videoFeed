import template from './feed.html';
import controller from './feed.controller';
import './feed.scss';

let feedComponent = {
  bindings: {},
  template,
  controller
};

export default feedComponent;
